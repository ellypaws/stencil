package imageprocessor

import (
	"image"
	_ "image/png" // Add other formats as needed.
)

func ProcessImage(filePath string) []image.Point {
	// Load the image
	// Extract pixels
	// Group them as required
	// Return the list of points to be drawn
	return nil
}
